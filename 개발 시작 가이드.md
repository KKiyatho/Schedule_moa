# 일정모아 개발 시작 가이드

## 🎯 목표
차근차근 하나하나씩 개발하면서 각 단계마다 테스트할 수 있도록 준비하기

---

## 📌 개발 단계별 가이드

### 🔵 **1단계: 백엔드 기초 세팅** (1-2시간)

#### 1-1. 환경 구성
```bash
# backend 폴더에서
cd backend

# 가상환경 생성 및 활성화
python -m venv venv
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 환경 변수 설정
copy .env.example .env  # Windows
# 또는
cp .env.example .env    # macOS/Linux
```

#### 1-2. 의존성 설치
```bash
pip install -r requirements.txt
```

#### 1-3. 서버 실행
```bash
uvicorn main:app --reload --port 8000
```

#### ✅ 테스트
**Swagger UI 접속:** http://localhost:8000/docs

다음을 확인:
- [x] 헬스 체크 응답 (GET /)
- [x] API 문서가 정상 표시되는가?
- [x] 모든 엔드포인트가 나열되는가?

**cURL 테스트:**
```bash
curl http://localhost:8000/health
```

**기대 응답:**
```json
{"status":"healthy","app":"일정모아","version":"1.0.0"}
```

---

### 🔵 **2단계: 프론트엔드 기초 UI** (1시간)

#### 2-1. Flutter 환경 확인
```bash
flutter doctor
```

#### 2-2. 프론트엔드 폴더에서 의존성 설치
```bash
cd frontend
flutter pub get
```

#### 2-3. 웹 실행
```bash
flutter run -d chrome
```

#### ✅ 테스트
다음을 확인:
- [x] 브라우저에서 '일정모아' 앱이 실행되는가?
- [x] Material 3 테마가 적용되는가?
- [x] 하단 네비게이션 바가 작동하는가?

**기대 화면:**
- 중앙에 '일정모아' 로고
- 하단에 4개 탭 (오늘, 일정, 할 일, 프로필)
- "시작하기" 버튼 클릭 시 스낵바 표시

---

### 🟡 **3단계: 백엔드-프론트엔드 연결** (1-2시간)

#### 3-1. API 테스트 (Swagger에서)

**POST /api/v1/auth/register 테스트:**
1. Swagger UI에서 "auth" 섹션 확장
2. "POST /api/v1/auth/register" 클릭
3. "Try it out" 버튼 클릭
4. 다음 JSON 입력:
```json
{
  "email": "test@example.com",
  "full_name": "테스트 사용자"
}
```
5. Execute 버튼 클릭

**기대 응답:**
```json
{
  "access_token": "eyJ0eXA...",
  "token_type": "bearer",
  "user": {
    "id": "uuid...",
    "email": "test@example.com",
    "full_name": "테스트 사용자",
    "is_active": true,
    "created_at": "2026-08-08T..."
  }
}
```

#### 3-2. 프론트엔드에서 연결 테스트
백엔드가 실행 중인 상태에서 프론트엔드도 실행:

```bash
# 터미널 1 (백엔드)
cd backend
source venv/bin/activate
uvicorn main:app --reload --port 8000

# 터미널 2 (프론트엔드)
cd frontend
flutter run -d chrome
```

#### ✅ 통합 테스트
다음을 확인:
- [x] 백엔드 서버 실행 중 (http://localhost:8000/docs)
- [x] 프론트엔드 앱 실행 중
- [x] API 클라이언트가 백엔드에 접근 가능

---

### 🟡 **4단계: 파일 업로드 기능** (2-3시간)

#### 4-1. 백엔드에서 테스트
**POST /api/v1/documents/upload 테스트:**

1. Swagger UI에서 "documents" 섹션 확장
2. "POST /api/v1/documents/upload" 클릭
3. "Try it out" 버튼 클릭
4. 파일 선택 (PDF 또는 이미지)
5. Execute

**기대 응답:**
```json
{
  "id": "uuid...",
  "file_name": "example.pdf",
  "file_type": "pdf",
  "status": "pending",
  "created_at": "2026-08-08T..."
}
```

#### 4-2. 업로드된 파일 조회
**GET /api/v1/documents/ 테스트:**

앞의 테스트 후 다음 요청:
```bash
curl http://localhost:8000/api/v1/documents/
```

**기대 응답:**
```json
[
  {
    "id": "uuid...",
    "file_name": "example.pdf",
    "file_type": "pdf",
    "status": "pending"
  }
]
```

#### ✅ 테스트 완료
- [x] 파일 업로드 성공
- [x] 파일 목록 조회 성공
- [x] 파일이 `uploads/` 폴더에 저장됨

---

### 🔴 **5단계: 일정 항목 생성** (1-2시간)

#### 5-1. 항목 생성 테스트
**POST /api/v1/items/ 테스트:**

Swagger에서 다음 JSON 입력:
```json
{
  "document_id": "이전_업로드_문서의_uuid",
  "title": "팀 미팅",
  "description": "월간 팀 미팅",
  "item_type": "schedule",
  "due_date": "2026-08-15",
  "due_time": "14:00",
  "location": "회의실 A",
  "priority": 4
}
```

**기대 응답:**
```json
{
  "id": "uuid...",
  "title": "팀 미팅",
  "item_type": "schedule",
  "due_date": "2026-08-15",
  "status": "pending",
  "is_completed": false,
  "created_at": "2026-08-08T..."
}
```

#### 5-2. 항목 목록 조회
**GET /api/v1/items/ 테스트:**

```bash
curl http://localhost:8000/api/v1/items/
```

또는 특정 타입만 조회:
```bash
curl "http://localhost:8000/api/v1/items/?item_type=schedule"
```

#### ✅ 테스트 완료
- [x] 일정/마감/할 일 생성 가능
- [x] 항목 목록 조회 가능
- [x] 필터링 작동

---

### 🔴 **6단계: 항목 완료 처리** (30분)

#### 6-1. 항목 완료 표시
**POST /api/v1/items/{item_id}/complete 테스트:**

Swagger에서:
1. 항목의 UUID 복사
2. "/items/{item_id}/complete" 엔드포인트에 UUID 입력
3. Execute

**기대 응답:**
```json
{
  "id": "uuid...",
  "is_completed": true,
  "completed_at": "2026-08-08T...",
  "status": "pending"
}
```

#### ✅ 테스트 완료
- [x] 항목 완료 처리 성공
- [x] completed_at 타임스탬프 기록
- [x] 실제 사용 가능성 확인

---

## 📊 전체 테스트 체크리스트

### 백엔드 ✅
- [ ] 서버 정상 실행
- [ ] Swagger UI 접속 가능
- [ ] Health check 응답
- [ ] 사용자 등록 API 작동
- [ ] 파일 업로드 API 작동
- [ ] 일정 항목 CRUD 작동
- [ ] 완료 처리 API 작동

### 프론트엔드 ✅
- [ ] 웹 브라우저에서 실행
- [ ] Material 3 테마 적용
- [ ] 네비게이션 탭 작동
- [ ] API 클라이언트 준비 완료

### 통합 ✅
- [ ] 백엔드 + 프론트엔드 동시 실행
- [ ] 로그 확인 가능 (터미널)
- [ ] API 응답 정상

---

## 🚀 다음 단계

각 단계를 완료한 후:

1. **AI 통합** - OpenAI API로 일정 자동 분류
2. **Google Calendar** - Calendar API 연동
3. **UI 고도화** - 로그인 화면, 대시보드 등
4. **알림 시스템** - 시간 알림 및 푸시 알림
5. **배포** - 모바일 빌드 및 클라우드 배포

---

## 🐛 문제 해결

### "포트 8000/59102 이미 사용 중"
```bash
# 다른 포트 사용
# 백엔드:
uvicorn main:app --reload --port 8001

# 프론트엔드:
flutter run -d chrome --port 5001
```

### "API 연결 실패"
- 백엔드 서버 실행 중인지 확인
- 방화벽에서 포트 8000 허용되는지 확인
- .env 파일의 DATABASE_URL 확인

### "의존성 설치 실패"
```bash
# 백엔드
pip cache purge && pip install -r requirements.txt --force-reinstall

# 프론트엔드
flutter clean && flutter pub get
```

---

## 📞 도움말

- 백엔드 문서: `backend/README.md`
- 프론트엔드 문서: `frontend/README.md`
- 기획서: `PRD.md`
- MVP 계획: `MVP.md`
- 개발 에이전트 지침: `Agent.md`

---

## ✨ 축하합니다! 🎉

모든 단계를 완료했다면, 기본적인 일정모아 앱이 동작하고 있습니다!

다음은 **AI 통합**과 **Google Calendar 연동**으로 진행합니다.

💪 화이팅!
