# 🧪 Schedule Moa - Flutter 크롬 테스트 명령어

## 🚀 빠른 시작 (Windows PowerShell)

### 터미널 1: Backend 서버
```powershell
cd c:\dev\Schedule_moa\backend
c:/dev/Schedule_moa/.venv/Scripts/python.exe -m uvicorn main:app --reload --host 0.0.0.0
```

**예상 출력:**
```
INFO:     Will watch for changes in these directories: ['C:\dev\Schedule_moa\backend']
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### 터미널 2: Flutter 앱
```powershell
cd c:\dev\Schedule_moa\frontend
flutter run -d chrome
```

---

## 🔗 브라우저 접속

| 용도 | URL |
|------|-----|
| **Swagger API 문서** | http://localhost:8000/docs |
| **Backend 헬스 체크** | http://localhost:8000/health |
| **Flutter 앱 (Chrome)** | http://localhost:59102 (포트는 Flutter가 자동 할당) |

---

## 📝 테스트 절차

### 1단계: 백엔드 확인
```powershell
# Swagger 문서 접속 (http://localhost:8000/docs)
# Try it out 버튼으로 /health 엔드포인트 테스트
```

### 2단계: 앱 로그인
1. Chrome에서 Flutter 앱 로드
2. 로그인 폼 입력:
   - **Email**: test@example.com
   - **Full Name**: Test User
3. **Sign In / Register** 클릭

### 3단계: 대시보드 확인
- 인사말: "Hello, Test!"
- 3개 요약 카드 확인
- Sidebar 네비게이션 확인
- FAB (Upload Document) 버튼 확인

---

## 🔧 문제 해결

### Backend: "ModuleNotFoundError"
```powershell
# 의존성 재설치
c:/dev/Schedule_moa/.venv/Scripts/python.exe -m pip install -r requirements.txt
```

### Flutter: 빌드 실패
```powershell
cd c:\dev\Schedule_moa\frontend
flutter clean
flutter pub get
flutter run -d chrome
```

### 포트 충돌 (이미 실행 중)
```powershell
# 포트 8000 사용 프로세스 찾기
netstat -ano | findstr :8000

# 프로세스 종료 (PID 확인 후)
taskkill /PID <PID> /F
```

---

## ⚙️ 환경 정보

| 항목 | 값 |
|------|-----|
| **Backend** | FastAPI 0.104.1 |
| **Database** | SQLite (schedule_moa.db) |
| **Frontend** | Flutter 3.10+ |
| **Design** | Material 3 |
| **API URL** | http://localhost:8000/api/v1 |
| **Python** | 3.14.4 |
| **venv** | c:/dev/Schedule_moa/.venv |

---

## 🔄 개발 중 Hot Reload

### Backend (uvicorn --reload 사용)
- main.py 수정 → 자동 재시작

### Frontend (Flutter)
- Dart 파일 수정 후 Chrome에서 **R** 누름 → 핫 리로드
- **Shift + R** → 핫 리스타트 (풀 재빌드)



